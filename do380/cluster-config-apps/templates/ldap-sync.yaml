apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: ldap-sync
  namespace: argocd
spec:
  destination:
    server: 'https://kubernetes.default.svc'
    namespace: {{ .Values.namespace }}
  project: {{ .Values.argocdProject }}
  source:
    path: "{{ .Values.targetEnv }}/ldap-sync"
    repoURL: "{{ .Values.repoUrl }}"
    targetRevision: HEAD